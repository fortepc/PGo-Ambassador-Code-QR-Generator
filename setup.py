from setuptools import setup, find_packages

setup(
    name='AmbassadorCodeQRGenerator',
    version='1.0',
    packages=find_packages(),
    install_requires=[
        # Add other dependencies if needed
    ],
)
